@model Ecommerce.Data.Voucher_User

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Voucher_User</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="id_voucher" class="control-label"></label>
                <select asp-for="id_voucher" class ="form-control" asp-items="ViewBag.Voucher"></select>
            </div>
            <div class="form-group">
                <label asp-for="id_user" class="control-label"></label>
                <select asp-for="id_user" class ="form-control" asp-items="ViewBag.User"></select>
            </div>
           
            
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
